const name = 'Deadpool';
const realName = 'Wade Winston';

const nameAndRealName = name + ' ' + realName;

console.log(nameAndRealName);

const template = `${ name } ${ realName }`;
console.log(template);

const html = `
    <h1>Hello</h1>
    <p>Word</p>
`;

console.log(html);